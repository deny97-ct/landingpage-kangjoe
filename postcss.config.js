module.exports = {
  plugins: {
    // Memproses direktif tailwind seperti @tailwind base, dsb.
    tailwindcss: {},
    // Menambahkan vendor prefix otomatis agar tampilan rapi di semua browser (Chrome, Safari, Firefox)
    autoprefixer: {},
  },
}
